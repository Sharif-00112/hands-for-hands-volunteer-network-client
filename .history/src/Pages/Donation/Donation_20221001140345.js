import React from 'react';
import { useTitle } from '../../hooks/useTitle';

const Donation = () => {
    useTitle("Donation");

    return (
        <div>
            <h2>Donation</h2>
        </div>
    );
};

export default Donation;